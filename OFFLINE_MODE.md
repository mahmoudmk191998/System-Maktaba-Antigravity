# Alwan Retail ERP — Offline-First Resilience & Automatic Sync Engine

وثيقة معمارية وتشغيلية تفصيلية لمحرك العمل دون اتصال بالإنترنت والمزامنة التلقائية الموثوقة.

---

## 1. المعمارية والركائز الأساسية (Architecture Core)

تم تصميم المحرك كطبقة مرنة غير متدخلة (**Resilient Pre-Commit Layer**) تعلو طبقة الخدمات الموثوقة القائمة (`completeSaleTransaction`, `completeSaleReturnTransaction`, `inventoryCount.service`) مع الالتزام الصارم بقاعدة **Zero Regression**.

### التدفق المعماري:
```text
User / Cashier Action
        │
        ▼
   Online Check
   (Dual Detection: Browser + Active Endpoint Probe)
   ┌────┴──────────────────────────┐
   │                               │
  YES                             NO
   │                               │
   ▼                               ▼
Existing Service             Offline Engine
(e.g., completeSaleTransaction)     │
   │                               ▼
   ▼                     Durable Command Queue (IndexedDB)
Firestore Transaction              │
   │                               ▼
   ▼                     Local Shadow State
Success / Error                    (Shadow Stock & Shift Cash)
                                   │
                                   ▼
                         Provisional Receipt (OFF-NC-XXXX)
                                   │
                           Internet Restored
                                   │
                                   ▼
                         Trusted Sync Engine
                      (Leader Lease via IndexedDB)
                                   │
                                   ▼
                         Existing Service Re-run
                         (Exact Original Idempotency Key)
                                   │
                         Firestore Transaction
                                   │
                        ┌──────────┴──────────┐
                        ▼                     ▼
                     Success               Conflict
                        │                     │
                        ▼                     ▼
                  Status: Synced       Status: Needs Review
                  Final Invoice Num     (Manager Sync Center)
```

---

## 2. جدول العمليات المدعومة والمقيدة (Operations Matrix)

| العملية (Operation) | القراءة المحلية (Offline Read) | مسودة محلية (Offline Draft) | التنفيذ النهائي دون اتصال (Offline Final) | سلوك المزامنة (Sync Behavior) |
|---|---|---|---|---|
| **بيع الكاشير النقدي (Cash Retail Sale)** | مدعوم (من الكاش) | نعم (سلة ومسودة) | مؤقت (Provisional Invoice) | مزامنة تلقائية وإصدار فاتورة رسمية |
| **بيع بالفيزا / محافظ (Card / Recorded)** | مدعوم (من الكاش) | نعم | مؤقت (شرط إتمامها بجهاز نقاط بيع خارجي) | مزامنة تلقائية |
| **بيع آجل (Credit Sales)** | مدعوم (قراءة الحدود) | مسودة فقط | ❌ غير مسموح افتراضياً | تتطلب التحقق المتزامن من السيرفر |
| **دفعات / أرصدة العملاء (Customer Balance)** | مدعوم (قراءة الرصيد) | مسودة فقط | ❌ غير مسموح افتراضياً | تتطلب اتصالاً مباشراً لمنع تجاوز الرصيد |
| **جرد المخازن (Physical Stocktake)** | مدعوم (فهرس الباركود) | نعم (حفظ محلي فوري) | نعم (جلسة محلية مؤكدة) | رفع القراءات والمطابقة المحاسبية سيرفر |
| **استلام البضائع (Goods Receipt Note)** | مدعوم (قراءة أوامر الشراء) | مسودة قراءات | ❌ غير مسموح بترحيل المخزن والـGL | ترحيل رسمي عبر الـAPI بعد المزامنة |
| **المرتجعات والاستبدال (Sales Returns)** | مدعوم (قراءة الفواتير) | مسودة مرتجع | ❌ غير مسموح بالصرف النهائي | تتطلب التحقق من عدم ردها من فرع آخر |
| **قيود اليومية المحاسبية (Journal Entries)** | مدعوم (قراءة الدليل) | مسودة فقط | ❌ غير مسموح بالترحيل المالي | ترحيل معتمد على الخادم |

---

## 3. الضمانات الست الصارمة (The Six Mandatory Safeguards)

1. **حالة `awaiting_confirmation` وقفل الإلغاء المحلي**:
   - إذا انقطع الاتصال أثناء إرسال الفاتورة أونلاين، تُحفظ العملية بحالة `awaiting_confirmation`.
   - **قاعدة حرجة**: لا يمكن للكاشير أو أي مستخدم إلغاء هذه العملية محلياً حتى يتم فحص السيرفر بنفس الـ `idempotencyKey` للتأكد هل تم ترحيلها قبل الانقطاع أم لا.
2. **عقد إيجار قيادة التبويبات المتعددة (IndexedDB Leader Lease)**:
   - منع تشغيل المزامنة من أكثر من تبويب مفتوح في نفس الوقت عبر جدول `offline_leases` بمدة صلاحية (10 ثوانٍ) ونبضات دورية (كل 4 ثوانٍ)، وتنبيهات فورية عبر `BroadcastChannel`.
3. **سلامة المعاملة المحلية قبل تأكيد الواجهة (Strict Atomic Integrity)**:
   - لا يتم مسح السلة (Cart) أو إظهار رسالة النجاح أو فتح إيصال الفاتورة إلا بعد اكتمال واطمئنان عملية الحفظ الذرية في IndexedDB (`offline_queue` + `shadow_stock` + `offline_shifts`). إذا فشل التخزين المحلي، تظل السلة كما هي دون أي فقدان.
4. **التمييز الدقيق بين أخطاء الشبكة وأخطاء الصلاحيات والمنطق التجاري**:
   - أخطاء الصلاحيات (`permission-denied`) أو انتهاء الجلسة (`unauthenticated`) أو قيود العمل (مثل نفاذ الرصيد) **لا** تُحوّل إطلاقاً لقائمة الانتظار، بل يتم إيقافها فوراً وإظهار رسالتها المحددة. فقط أخطاء انقطاع الاتصال الحقيقية هي التي تُخزن في قائمة الـOffline.
5. **فحص سلامة وجاهزية الذاكرة المحلية (`offlineCacheReady`)**:
   - يُمنع الكاشير من البيع دون اتصال إذا لم يكن الجهاز قد أكمل سابقاً مزامنة كتالوج المنتجات والأسعار وأرصدة الفرع، مع إظهار تنبيه توجيهي واضح.
6. **إيصالات مؤقتة عالية العشوائية والفرادة (High-Entropy Provisional Receipts)**:
   - أرقام الإيصالات المؤقتة تكون بالصيغة: `OFF-{branchCode}-{deviceShort}-{timestamp36}-{entropy}`، وتُطبع وتُعرض مع ختم مائي صريح: **"إيصال مؤقت — غير متزامن — بانتظار المزامنة"** لمنع تزوير الفواتير الضريبية.

---

## 4. نموذج المخزون الظلي المحلي (Local Shadow Stock)

- لتجنب بيع نفس الكمية أكثر من مرة على نفس الجهاز أثناء انقطاع الإنترنت:
  - عند كل عملية بيع محلياً، يتم فوراً خصم الكمية من `shadow_stock` محلياً.
  - إذا توفر في الفرع 5 قطع، وباع الكاشير 3 قطع، يصبح الرصيد المحلي المتاح فوراً 2 قطعة.
  - أي محاولة لبيع 3 قطع أخرى على نفس الجهاز تُرفض محلياً لعدم كفاية الرصيد المحلي.

---

## 5. مركز المزامنة وإدارة التعارضات (Sync Center)

يُفتح مركز المزامنة من أعلى شريط النظام أو من داخل نقطة البيع عبر مؤشر الاتصال، ويتيح:
1. **العمليات المعلقة**: متابعة العمليات المحفوظة ومقدار المبالغ المحجوزة.
2. **العمليات غير المؤكدة (`awaiting_confirmation`)**: العمليات التي فُقد اتصالها في منتصف الإرسال لمنع التكرار، مع قفل زر الإلغاء المحلي حتى مراجعة الخادم.
3. **التعارضات (Conflicts)**: في حال باع فرع آخر القطع المتبقية قبل عودة هذا الجهاز، تظهر العملية في التعارضات بالتفصيل (الكمية المطلوبة vs الكمية المتبقية بالسيرفر) لاتخاذ القرار الإداري دون إفساد المخزون.
4. **تنبيه مالي حرج للمبالغ النقدية المحصلة**: إذا كانت الفاتورة التي دخلت في تعارض قد تم تحصيل نقدية عنها بالدرج، يُعرض تحذير بارز يمنع الحذف ويفرض مراجعة الإدارة المالية لحل المخزون دون المساس بالحق المالي.
5. **المزامنة اليدوية**: زر "مزامنة الآن" لإعادة إرسال العمليات فور توفر الاتصال.

---

## 6. نموذج التنفيذ والموثوقية (Execution & Trust Model)

- **طبيعة `completeSaleTransaction`**:
  - تُنفذ `completeSaleTransaction` كـ **Client Firestore Transaction** (`runTransaction(db, ...)`) وتخضع لـ **Firestore Security Rules**.
  - **ليست** Cloud Function خلفية معزولة، ولذا لا يُطلق عليها "Trusted Server Function" دون وجود حماية صارمة بالقواعد.
- **تأمين الثوابت والقواعد الصارمة (Firestore Security Rules Enforcement)**:
  - **العزل المؤسسي (`tenantId`)**: تمنع القواعد أي قراءة أو كتابة لا تتطابق مع `request.auth.token.tenant_id`.
  - **سلامة المخزون (`branch_stock`)**: اشتراط ألا يكون الرصيد الناتج سالبًا (`after.onHandQuantity >= 0`).
  - **منع التلاعب بالأسعار**: الفواتير يجب أن تتطابق مع الحد الأدنى للبيع المسموح به للصنف ما لم يكن المستخدم يمتلك صلاحية `admin`.
  - **الورديات النقدية (`cashier_shifts`)**: تمنع القواعد ترحيل أي مبيعات إذا كانت الوردية مغلقة (`status == 'open'`).
  - **مستندات منع التكرار (`idempotency_locks`)**: لا يمكن تكرار تسجيل مستند بنفس `clientCheckoutId` مرتين.

---

## 7. العزل المؤسسي وتعدد المستخدمين محلياً (Multi-Tenant & User Isolation in IndexedDB)

- **عزل المؤسسات (`Tenant Isolation`)**:
  - جميع جداول IndexedDB (`catalog_cache`, `shadow_stock`, `offline_queue`, `offline_shifts`, `offline_counts`) مفهرسة بحقل `tenantId`.
  - عند تبديل الحساب إلى مؤسسة أخرى (Tenant B)، يُمنع قراءة أو البحث في كتالوج أو مخزون أو عمليات المؤسسة (Tenant A).
- **عزل المستخدمين (`User Isolation`)**:
  - كل عملية غير متزامنة تسجل `createdByUserId`.
  - إذا سجل الكاشير A خروجه ودخل الكاشير B:
    - العمليات المالية المعلقة لا تُحذف لأنها أصل مؤسسي.
    - يُمنع الكاشير B من تعديل أو إلغاء عمليات الكاشير A ما لم يمتلك صلاحية مدير (`manager` أو `admin`).
    - تسجل عمليات الإلغاء والقرارات الإدارية في سجل تدقيق محلي (`audit_cancel_${opId}`) يوضح الفاعل والسبب والتاريخ.

---

## 8. واقع أجهزة عدم الاتصال المتعددة ومعالجة التعارضات (Multi-Device Reality & Conflict Invariants)

- **استحالة منع الـ Overselling التام عالمياً أثناء عدم الاتصال**:
  - إذا كان رصيد الصنف بالسيرفر (5 قطع)، ودخل جهازان A و B في وضع عدم الاتصال:
  - باع جهاز A (5 قطع) وباع جهاز B (5 قطع) محلياً.
  - عند عودة الاتصال: ينجح جهاز A في الترحيل، بينما يُرفض جهاز B بسبب نفاذ الرصيد السحابي ويدخل حالة **تعارض (`conflict`)**.
- **ضمانات النظام الصارمة**:
  - **ممنوع المخزون السالب**: لا يتم ترحيل مبيعات B تلقائياً برصيد سالب.
  - **حماية الأموال المحصلة**: لا تُحذف عملية B من الجهاز، بل تظل محفوظة في مركز المزامنة تحت بند "تنبيه مالي حرج".
  - **ممنوع Force Sync متجاوز للمخزون**: لا يوجد زر يجبر الخادم على قبول رصيد غير موجود، بل يلزم توريد/تسوية مخزنية نظامية أولاً ثم إعادة المزامنة.

---

## 9. سياسة الوردية والفترات المحاسبية والتسعير (Shift, Period & Price Policy)

- **سلامة إغلاق الوردية**:
  - يُمنع الكاشير من إغلاق الوردية نهائياً إذا وُجدت مبيعات معلقة دون اتصال مرتبطة بنفس الوردية حتى تتم مزامنتها.
- **إغلاق الوردية عن بُعد**:
  - إذا أغلقت الإدارة الوردية من جهاز آخر أثناء انقطاع الجهاز A، تُرفض المبيعات المعلقة وتدخل في التعارض لمنع ترحيل مالي إلى وردية مغلقة.
- **انحراف الأسعار (Price Drift)**:
  - إذا تغير سعر الصنف في السيرفر أثناء انقطاع الجهاز، يلتزم السيرفر بالحدود الدنيا للصنف وصلاحيات السعر، وتُعرض للمراجعة إذا خالفت السياسات المعتمدة.

---

## 10. الأداء والحجم التخزيني والأمان (Performance & Storage Estimates)

- **تقديرات استهلاك IndexedDB**:
  - **5,000 صنف**: ~ 2.5 ميجابايت.
  - **10,000 صنف**: ~ 5.0 ميجابايت.
  - **100 عملية معلقة**: ~ 350 كيلوبايت.
- **تقليل البيانات (Data Minimization)**:
  - لا يتم تخزين صور المنتجات داخل IndexedDB إطلاقاً (فقط روابط URL).
  - لا يتم تنزيل كشوف الحسابات السرية للعملاء أو الموردين أو قيود اليومية المحاسبية بالكامل، بل فقط الاسم والمعرف والحد الائتماني.
- **تحديثات PWA والأمان**:
  - عند توفر تحديث للـ PWA، يُمنع إعادة التحميل القسري (`force reload`) أثناء وجود سلة نشطة أو كتابة جارية في قاعدة البيانات المحلية.
  - ترقيات Schema تحافظ على جدول `offline_queue` وتستخدم `schemaVersion` و `operationVersion` لضمان عدم ضياع أي عملية.
